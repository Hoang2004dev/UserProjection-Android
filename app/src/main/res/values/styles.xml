<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Button style -->
    <style name="Widget.UserProtection.Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/purple_500</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- EditText style -->
    <style name="Widget.UserProtection.EditText" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="android:padding">12dp</item>
    </style>

    <!-- TextView Title style -->
    <style name="Widget.UserProtection.TitleText" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">@color/purple_700</item>
    </style>

</resources>
